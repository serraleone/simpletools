package com.example.test1;

/**
 * @author WEN_PENGFEI
 * @version 1.0 2021/10/16
 */


public class FileReaderServiceTest {
	/*
	public static String url;
	
	public static void main(String[] args) {
	
		//検索したいフォルダーのパス
		
		url = "C:\\Users\\Wen\\Desktop\\new\\__pycache__\\java\\test2";
		/*
		DetectFileEncoding dfe = new DetectFileEncoding();
	        dfe.detectCode(url);
	        
		FileReaderService fileReaderServiceImpl = new FileReaderServiceImpl();
		ArrayList<FileInfo> fileList = new ArrayList<FileInfo>();
		int calPhyStep = 0;
		int calLogStep = 0;

		
        
            fileList = fileReaderServiceImpl.findAllFilesInFolder(new File(url));
			
            if(fileList.size() == 0) {
				throw new BusinessException(); 
			}
			
            for(FileInfo fileInfo : fileList) {
            	System.out.print(fileInfo.getFileName() + " | ");
            	System.out.print(fileInfo.getUrl() + " | ");
            	System.out.print(fileInfo.getPhyStep() + " | ");
            	System.out.print(fileInfo.getLogStep());
            	System.out.println();
            	calPhyStep += fileInfo.getPhyStep();
            	calLogStep += fileInfo.getLogStep();
            }
            
            ArrayList<Integer> calList = new ArrayList<Integer>();
            calList.add(0,calPhyStep);
            calList.add(1,calLogStep);
            System.out.println("合計物理的ステップ数；" + calPhyStep + "行" );
            System.out.println("合計論理的ステップ数；" + calLogStep + "行");
            
            fileReaderServiceImpl.fileListOutput(url, fileList, calList);
               
	  
	}

  */	
}
